image: node:13.7.0

before_script:
- npm install

cache:
  paths:
    - node_modules/

lint:
  script:
    - npm run lint

test:
  script:
    - npm run build
    - npm run test

release:
  only:
    - tags
  script:
    - npm run build
  artifacts:
    paths:
      - dist/src
