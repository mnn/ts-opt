{
  "name": "ts-opt",
  "version": "0.0.1",
  "description": "Typed Option/Maybe monad for TypeScript and JavaScript.",
  "main": "dist/src/Opt.js",
  "author": "monnef",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "lint": "tslint src/*.ts",
    "pretest": "npm run lint",
    "test": "nyc -r html -e .ts -x \"test/*.ts\" mocha -r ts-node/register test/**/*.ts && nyc report --check-coverage --branches 100 --functions 100 --lines 100 --statements 100",
    "doc": "typedoc --out doc src",
    "prebuild": "npm run clean && npm run test && npm run doc",
    "build": "tsc --outDir ./dist -p . ",
    "precommit": "npm run build"
  },
  "devDependencies": {
    "@types/chai": "4.1.7",
    "@types/chai-spies": "1.0.0",
    "@types/mocha": "5.2.6",
    "chai": "4.2.0",
    "chai-spies": "1.0.0",
    "husky": "1.3.1",
    "mocha": "6.0.2",
    "nyc": "13.3.0",
    "rimraf": "2.6.3",
    "ts-node": "8.0.3",
    "tslint": "5.14.0",
    "typedoc": "0.14.2",
    "typescript": "3.3.3333"
  }
}
