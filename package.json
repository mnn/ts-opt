{
  "name": "ts-opt",
  "version": "0.0.7",
  "description": "Typed Option/Maybe for TypeScript and JavaScript, created to simplify code involving optional values.",
  "keywords": [
    "option",
    "maybe",
    "optional",
    "fp",
    "functional programming",
    "typescript",
    "monad"
  ],
  "main": "dist/src/Opt.js",
  "types": "dist/src/Opt.d.ts",
  "author": "monnef",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "lint": "tslint src/*.ts",
    "pretest": "npm run lint",
    "test": "nyc -r html -e .ts -x \"test/*.ts\" mocha -r ts-node/register test/**/*.ts && nyc report --check-coverage --branches 100 --functions 100 --lines 100 --statements 100",
    "doc": "typedoc --out doc --target es5 --module node src",
    "prebuild": "npm run clean && npm run test",
    "build": "tsc --outDir ./dist -p . ",
    "precommit": "npm run build && npm run doc",
    "prepush": "npm run build && npm run doc",
    "prepublish": "npm run build && npm run doc"
  },
  "husky": {
    "pre-commit": "npm run precommit",
    "pre-push": "npm run prepush"
  },
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/monnef/ts-opt"
  },
  "devDependencies": {
    "@types/chai": "4.1.7",
    "@types/chai-spies": "1.0.0",
    "@types/mocha": "5.2.6",
    "chai": "4.2.0",
    "chai-spies": "1.0.0",
    "husky": "1.3.1",
    "mocha": "6.0.2",
    "nyc": "13.3.0",
    "rimraf": "2.6.3",
    "ts-node": "8.0.3",
    "tslint": "5.14.0",
    "typedoc": "0.14.2",
    "typescript": "3.3.3333"
  }
}
